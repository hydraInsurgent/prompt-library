# {{PROJECT_NAME}} Product Design

This document describes what {{PROJECT_NAME}} is today and the principles behind it.
It is a reference point, not a constraint. If a proposed feature or direction
differs from what is written here, that is a signal to have a conversation
and update this document - not to automatically reject the idea.

---

## What {{PROJECT_NAME}} Is

<!-- 2-4 sentences. What problem does it solve? Who is it for? What makes it distinct?
e.g. "A task management CLI for solo developers who want to track work without leaving the terminal." -->
{{PRODUCT_DESCRIPTION}}

---

## The User

<!-- Describe the primary user: role, context, goals, frustrations.
e.g. "A solo developer working on side projects. Wants to move fast, hates context-switching, values tools that stay out of the way." -->
{{USER_PROFILE}}

---

## Product Principles

<!-- Bullet list of guiding principles that shape decisions.
e.g.
- "Speed over features: one fast action beats three slow ones"
- "No surprises: the tool does exactly what the user said, nothing more"
- "Offline-first: never require a network connection for core workflows"
-->
{{PRODUCT_PRINCIPLES}}

---

## Current Scope

This is what {{PROJECT_NAME}} does today. Items listed here are not permanent limits -
they reflect where the product is right now and what assumptions the code makes.

<!-- Bullet list of what currently exists and works.
e.g.
- "CLI only - no web UI"
- "Single user - no auth or multi-tenancy"
- "Local file storage - no database"
-->
{{CURRENT_SCOPE}}

---

## How Features Currently Work

<!-- Brief description of each major feature. One paragraph or bullet list per feature.
e.g.
### Task creation
User runs `task add "description"`. The task is appended to tasks.json with a generated ID and timestamp.
-->
{{FEATURE_DETAILS}}
